// 3 - Crie um algoritmo que lê uma palavra e imprime as letras separadamente e cada letra em caixa alta (maíscula)

let palavra = 'palavra'

// Vou precisar iterar sobre a string palavra:

for (letra of palavra) {
    console.log(letra.toUpperCase())
}
